# Streamlit

the last pipeline runs a bash task that will start the streamlit server

> streamlit run streamlit_app.py --server.port=8501 --server.address=0.0.0.0

# db setup

- security for all the init files
- set up neon4j
